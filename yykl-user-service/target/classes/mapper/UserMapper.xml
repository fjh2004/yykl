<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yykl.user.mapper.UserMapper"> <!-- 对应UserMapper接口的全类名 -->

    <!-- 按用户名查询用户 -->
    <select id="selectByUsername" resultType="com.yykl.user.model.entity.User">
        SELECT * FROM yykl_user WHERE username = #{username}
    </select>

    <!-- 按用户ID查询角色 -->
    <select id="selectRolesByUserId" resultType="com.yykl.user.model.entity.Role">
        SELECT r.* FROM yykl_role r
                            JOIN t_user_role_relation ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 按用户ID查询权限 -->
    <select id="selectPermissionsByUserId" resultType="com.yykl.user.model.entity.Permission">
        SELECT p.* FROM yykl_permission p
                            JOIN t_role_permission_relation rp ON p.id = rp.permission_id
                            JOIN t_user_role_relation ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>