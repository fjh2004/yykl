<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yykl.product.mapper.ProductStockLogMapper">

    <!-- 按订单ID查询库存操作日志 -->
    <select id="selectByOrderId" parameterType="java.lang.String" resultType="com.yykl.product.model.entity.ProductStockLog">
        SELECT id, product_id, sku_id, operation_type, quantity, order_id, create_time
        FROM product_stock_log
        WHERE order_id = #{orderId}
        ORDER BY create_time DESC
    </select>

</mapper>