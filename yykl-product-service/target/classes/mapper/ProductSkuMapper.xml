<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yykl.product.mapper.ProductSkuMapper">

    <!-- 按产品ID查询所有SKU -->
    <select id="selectByProductId" parameterType="java.lang.Long" resultType="com.yykl.product.model.entity.ProductSku">
        SELECT id, product_id, name, price, spec
        FROM product_sku
        WHERE product_id = #{productId}
    </select>

</mapper>